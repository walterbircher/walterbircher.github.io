--- update production ---

git add .github/workflows/pages.yml .gitignore
git commit -m "Deploy site via GitHub Actions to Pages"
git push

--- run it locally ---

sudo bundle exec jekyll serve

localhost:4000